/**
 * Vercel Serverless Functions Configuration
 * 
 * These API routes handle OAuth and job fetching for connected platforms.
 * Deploy to Vercel for serverless execution.
 */

export const config = {
    runtime: 'edge', // Use Edge runtime for faster responses
};